<div class="flex items-start justify-between">
  <div class="flex-1">
    <div class="flex items-center gap-3 mb-2">
      <svg class="w-5 h-5 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5.882V19.24a1.76 1.76 0 01-3.417.592l-2.147-6.15M18 13a3 3 0 100-6M5.436 13.683A4.001 4.001 0 017 6h1.832c4.1 0 7.625-1.234 9.168-3v14c-1.543-1.766-5.067-3-9.168-3H7a3.988 3.988 0 01-1.564-.317z"></path>
      </svg>
      <h3 class="text-lg font-semibold text-gray-900">
        <%= link_to item.title, 
            announcement_path(item), 
            class: "hover:text-blue-600 transition-colors" %>
      </h3>
      
      <% if item.respond_to?(:priority) && item.priority.present? %>
        <% priority_class = case item.priority
                           when 'high' then "bg-red-100 text-red-800"
                           when 'medium' then "bg-yellow-100 text-yellow-800"
                           when 'low' then "bg-green-100 text-green-800"
                           else "bg-gray-100 text-gray-800"
                           end %>
        <span class="px-2 py-1 text-xs font-medium rounded-full <%= priority_class %>">
          <%= item.priority.humanize %> Priority
        </span>
      <% end %>
    </div>
    
    <% if item.content.present? %>
      <p class="text-gray-700 mb-3 line-clamp-3">
        <%= highlight(truncate(strip_tags(item.content), length: 200), query, 
            highlighter: '<mark class="bg-yellow-200 px-1 rounded">\1</mark>') %>
      </p>
    <% end %>
    
    <div class="flex items-center gap-4 text-sm text-gray-500">
      <div class="flex items-center gap-1">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
        </svg>
        <%= item.user.name if item.respond_to?(:user) && item.user %>
      </div>
      
      <% if item.respond_to?(:department) && item.department %>
        <div class="flex items-center gap-1">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
          </svg>
          <%= item.department.name %>
        </div>
      <% end %>
      
      <% if item.respond_to?(:published_at) && item.published_at %>
        <div class="flex items-center gap-1">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          Published <%= time_ago_in_words(item.published_at) %> ago
        </div>
      <% end %>
      
      <% if item.respond_to?(:expires_at) && item.expires_at %>
        <div class="flex items-center gap-1">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3a2 2 0 012-2h4a2 2 0 012 2v4m-6 0V6a2 2 0 012-2h4a2 2 0 012 2v1M8 7v13a2 2 0 002 2h4a2 2 0 002-2V7M8 7h8"></path>
          </svg>
          <% if item.expires_at < Time.current %>
            <span class="text-red-600">Expired</span>
          <% else %>
            Expires <%= time_ago_in_words(item.expires_at) %>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
  
  <div class="ml-4 flex-shrink-0">
    <div class="flex items-center gap-2">
      <%= link_to announcement_path(item), 
          class: "text-blue-600 hover:text-blue-800 text-sm font-medium" do %>
        View →
      <% end %>
    </div>
  </div>
</div>