<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-600 to-indigo-800 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8">
    <!-- Header -->
    <div class="text-center">
      <div class="mx-auto h-16 w-16 bg-white rounded-full flex items-center justify-center shadow-lg">
        <svg class="h-10 w-10 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
        </svg>
      </div>
      <h2 class="mt-6 text-4xl font-extrabold text-white">
        Admin Portal
      </h2>
      <p class="mt-2 text-sm text-indigo-100">
        Uni-Hub Administration Dashboard
      </p>
      <p class="mt-1 text-xs text-indigo-200">
        Restricted Access - Administrators Only
      </p>
    </div>

    <!-- Flash Messages -->
    <% if flash[:alert] %>
      <div class="rounded-md bg-red-50 p-4 border border-red-200">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <div class="ml-3">
            <p class="text-sm text-red-800"><%= flash[:alert] %></p>
          </div>
        </div>
      </div>
    <% end %>

    <% if flash[:notice] %>
      <div class="rounded-md bg-green-50 p-4 border border-green-200">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <div class="ml-3">
            <p class="text-sm text-green-800"><%= flash[:notice] %></p>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Login Form -->
    <div class="bg-white rounded-lg shadow-2xl">
      <div class="px-8 py-10">
        <%= form_for(resource, as: resource_name, url: admin_session_path, html: { class: "space-y-6" }) do |f| %>
          <!-- Email Field -->
          <div>
            <%= f.label :email, "Email Address", class: "block text-sm font-semibold text-gray-700 mb-2" %>
            <div class="mt-1 relative">
              <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207" />
                </svg>
              </div>
              <%= f.email_field :email, 
                  autofocus: true, 
                  autocomplete: "email",
                  placeholder: "admin@unihub.edu",
                  class: "appearance-none block w-full pl-10 pr-3 py-3 border border-gray-300 rounded-lg placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent text-gray-900" %>
            </div>
          </div>

          <!-- Password Field -->
          <div>
            <%= f.label :password, "Password", class: "block text-sm font-semibold text-gray-700 mb-2" %>
            <div class="mt-1 relative">
              <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
                </svg>
              </div>
              <%= f.password_field :password, 
                  autocomplete: "current-password",
                  placeholder: "Enter your password",
                  class: "appearance-none block w-full pl-10 pr-3 py-3 border border-gray-300 rounded-lg placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent text-gray-900" %>
            </div>
          </div>

          <!-- Remember Me -->
          <% if devise_mapping.rememberable? %>
            <div class="flex items-center">
              <%= f.check_box :remember_me, class: "h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded" %>
              <%= f.label :remember_me, class: "ml-2 block text-sm text-gray-700" do %>
                Remember me on this device
              <% end %>
            </div>
          <% end %>

          <!-- Submit Button -->
          <div>
            <%= f.submit "Sign In to Admin Portal", 
                class: "group relative w-full flex justify-center py-3 px-4 border border-transparent text-sm font-semibold rounded-lg text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors duration-200 shadow-lg" %>
          </div>
        <% end %>
      </div>

      <!-- Footer -->
      <div class="px-8 py-4 bg-gray-50 border-t border-gray-200 rounded-b-lg">
        <div class="text-center">
          <p class="text-xs text-gray-600">
            <svg class="inline h-4 w-4 text-gray-500 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
            </svg>
            This is a secure area. All access attempts are logged.
          </p>
          <p class="text-xs text-gray-500 mt-2">
            Not an admin? <%= link_to "Go to student portal", root_path, class: "text-indigo-600 hover:text-indigo-800 font-medium" %>
          </p>
        </div>
      </div>
    </div>

    <!-- Additional Security Notice -->
    <div class="text-center">
      <p class="text-xs text-indigo-200">
        © <%= Time.current.year %> Uni-Hub. All rights reserved.
      </p>
    </div>
  </div>
</div>
