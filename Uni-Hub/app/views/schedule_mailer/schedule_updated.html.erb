<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 30px;
      border-radius: 10px 10px 0 0;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 24px;
    }
    .content {
      background: #ffffff;
      padding: 30px;
      border: 1px solid #e5e7eb;
      border-top: none;
    }
    .schedule-card {
      background: #f9fafb;
      border-left: 4px solid #f59e0b;
      padding: 20px;
      margin: 20px 0;
      border-radius: 5px;
    }
    .changes-list {
      background: #fef3c7;
      padding: 15px;
      border-radius: 5px;
      margin: 15px 0;
    }
    .change-item {
      margin: 10px 0;
      padding: 10px;
      background: white;
      border-radius: 3px;
    }
    .button {
      display: inline-block;
      background: #3b82f6;
      color: white;
      padding: 12px 24px;
      text-decoration: none;
      border-radius: 5px;
      margin-top: 20px;
    }
    .footer {
      background: #f9fafb;
      padding: 20px;
      border-radius: 0 0 10px 10px;
      text-align: center;
      font-size: 12px;
      color: #6b7280;
      border: 1px solid #e5e7eb;
      border-top: none;
    }
    .alert {
      background: #fef3c7;
      border-left: 4px solid #f59e0b;
      padding: 15px;
      margin: 20px 0;
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>📅 Schedule Updated</h1>
  </div>

  <div class="content">
    <p>Hi <%= @student.full_name %>,</p>

    <div class="alert">
      <strong>Important:</strong> Your class schedule has been updated.
    </div>

    <div class="schedule-card">
      <h2 style="margin-top: 0; color: #1f2937;"><%= @schedule.title %></h2>
      <p style="color: #6b7280; font-size: 16px;"><%= @schedule.course %></p>

      <% if @changes.present? %>
        <div class="changes-list">
          <strong>What changed:</strong>
          <% @changes.each do |field, change| %>
            <div class="change-item">
              <strong><%= field.to_s.titleize %>:</strong><br>
              <span style="color: #dc2626; text-decoration: line-through;"><%= change[0] %></span>
              →
              <span style="color: #16a34a;"><%= change[1] %></span>
            </div>
          <% end %>
        </div>
      <% end %>

      <h3 style="margin-top: 20px;">Current Schedule:</h3>
      <p><strong>Day:</strong> <%= @schedule.day_name %></p>
      <p><strong>Time:</strong> <%= @schedule.formatted_time_range %></p>
      <p><strong>Room:</strong> <%= @schedule.room %></p>
      <p><strong>Instructor:</strong> <%= @instructor_name %></p>
    </div>

    <p>Please make note of these changes and adjust your schedule accordingly.</p>

    <a href="<%= schedule_url(@schedule) %>" class="button">View Full Schedule</a>
  </div>

  <div class="footer">
    <p>This is an automated notification from Uni-Hub.</p>
    <p>&copy; <%= Time.current.year %> Uni-Hub. All rights reserved.</p>
  </div>
</body>
</html>
