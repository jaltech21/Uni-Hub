<div class="container mx-auto px-4 py-8 max-w-2xl">
  <%= link_to "← Back to Department", admin_department_path(@department), class: "text-blue-600 hover:text-blue-800 mb-6 inline-block" %>
  
  <h1 class="text-3xl font-bold text-gray-900 mb-6">Edit Department</h1>

  <div class="bg-white rounded-lg shadow-md p-6">
    <%= render 'form', department: @department %>
  </div>
</div>
